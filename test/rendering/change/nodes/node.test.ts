import { describe, expect, it } from 'vitest'
import { compileForNode } from '../../../../lib/rendering/change/node'
import { createTestDom } from '../../../support/test-dom'
import { ChangeInstructionTypes } from '../../../../lib/types'

describe('compileForNode instructions', () => {
  it('compiles text nodes', () => {})
  it('compiles svg nodes with attributes', () => {})
  it('compiles element nodes with attributes and events', () => {})
})
